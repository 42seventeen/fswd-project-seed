extends ../layout.jade
block body
  h1=  title
  ul
    each user in users
      li
        strong= user.name
        br
        | #{user.role} -
        a(href="/users/detail/" + user.id)
          |  Details
